# Platform Assets

This directory contains OS-specific deployment and service integration assets
for `socks2proxy`.

Structure:

- `linux/`: systemd, AppArmor, and Linux helper scripts
- `darwin/`: launchd templates and helper scripts
- `windows/`: Windows service templates and helper scripts

These assets are examples and may require local path/account policy
adjustments.
