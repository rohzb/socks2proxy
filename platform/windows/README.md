# Windows Platform Assets

Windows deployment assets for `socks2proxy`.

Included:

- `service/socks2proxy-service.ps1`: service registration helper
- `scripts/install-service.ps1`: install/start helper wrapper
- `scripts/uninstall-service.ps1`: stop/remove helper wrapper

These files are templates and should be adapted to local policy.
